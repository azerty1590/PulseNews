rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the server-side Admin SDK — block direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
